<!DOCTYPE html>
<html lang="en">
    <head>
        <title>아리봇 명령어 리스트</title>
    </head>

    <body>
        <a href="/"><img class="avatar" src="https://cdn.discordapp.com/avatars/877221117046190090/2cfe4b0b7152adef03a29fa3a4df63ef.png?size=128"
            alt="Ara Avatar"></a>
        <section class="main-content">
            <p class="help">아라봇의 명령어를 살펴보아요! 사용법: 아라 (명령어)</p>

            <% commands.forEach(({ name, value }) => { %>
                <table style="width: 100%">
                    <h1 style="text-align: center;">
                        <%= name %>
                    </h1>
                    <tr>
                        <th>명령어 이름</th>
                        <th>사용방법</th>
                        <th>설명</th>
                        <th>그외 명령어</th>
                    </tr>
                    <% value.forEach(({ name, description, aliases, usage}) => { %>
                        <tr>
                            <td><%= name%></td>
                            <td><%= usage%></td>
                            <td><%= description%></td>
                            <td><%= typeof aliases === 'string' ? aliases : aliases.join(', ')%></td>
                        </tr>
                    <% }) %>
                </table>
            <%}) %>
        </section>
    </body>

    <style>
        table {
            border-collapse: collapse;
            border-spacing: 0;
        }
        * {
            box-sizing: border-box;
        }
        body {
            padding: 0;
            margin: 0;
            font-family: 'Jua', sans-serif;
            font-size: 16px;
            line-height: 1.5;
            color: black;
        }
        .main-content {
            word-wrap: break-word;
        }
        @media screen and (min-width: 42em) and (min-width: 64em) {
            .main-content{
                padding: 2rem 4rem;
                font-size: 1.1rem;
            }
        }

        @media screen and (min-width: 42em) {
            .main-content{
                padding: 2rem 1rem;
                font-size: 1rem;
            }
        }

        .main-content h1,
        .main-content h2,
        .main-content h3,
        .main-content h4,
        .main-content h5,
        .main-content h6 {
            margin-top: 2rem;
            margin-bottom: 1rem;
            font-family: 'Jua', sans-serif;
            font-weight: normal;
            color: blueviolet
        }

        .main-content table th,
        .main-content table td {
            padding: 0.5rem 1rem;
            border: 1px solid #e9ebec;
        }
        .avatar {
        display: block;
        margin-left: auto;
        margin-right: auto;
        border: 4px solid blueviolet;
        border-radius: 100%;
        width: 20%;
        height: 20%;
        -webkit-transform: -webkit-transform .8s ease-in-out;
        transform: transform .8s ease-in-out;
    }
    .avatar:hover {
        -webkit-transform: rotate(360deg);
        transform: rotate(360deg);
    }
    .help {
        font-size: 120%;
        font-family: 'Jua', sans-serif;
        color: black;
        text-align: center;
    }
    </style>
</html>